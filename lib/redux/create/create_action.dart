class CreateAction{

  final String text;
  // action is for mainlly pass the value form user
  // it contains value and pass it through reducer and app state can
  // use it when calls

  CreateAction(this.text);
}